# Dinner Party — Development Notes

Recording overlay fixes have been applied:
- Overlay only appears when recording starts successfully (was already in place)
- Dismiss/close button added to overlay top-right corner as safety fallback
- Alert shown when microphone is unavailable (simulator or denied permissions)
